# Stub secrets file for CI evaluation only. Not encrypted, contains no
# real secrets. The real, encrypted secrets.yaml lives in a separate private
# repo — see hosts/framework/README.md for how it's wired in.
placeholder: stub
