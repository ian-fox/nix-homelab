# Stub secrets file for CI evaluation only. Not encrypted.
# For real deployments, override the `secrets` input in flake.nix:
#   nixos-rebuild switch --flake .#forum \
#     --override-input secrets git+ssh://git@github.com/OWNER/nix-secrets
ifox-password: stub
ifox-ssh-key: stub
